- hosts: localhost
  roles:
    - role: add-fileserver
      fileserver: "{{ site_logs }}"
    - role: ara-report

- hosts: "{{ site_logs.fqdn }}"
  gather_facts: false
  roles:
    - role: upload-logs
      zuul_log_url: http://162.253.55.78/logs/windmill
